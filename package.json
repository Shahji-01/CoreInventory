{
  "name": "coreinventory",
  "version": "1.0.0",
  "description": "CoreInventory — MERN SaaS Inventory Management",
  "private": true,
  "scripts": {
    "dev": "concurrently \"npm run dev --prefix server\" \"npm start --prefix client\"",
    "install:all": "npm install --prefix server && npm install --prefix client",
    "start:server": "npm run dev --prefix server",
    "start:client": "npm start --prefix client"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
